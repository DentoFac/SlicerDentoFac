[pytest]
python_files = *TestCase.py
markers =
    slow: real inference or network integration test; excluded from the fast Slicer tier.
    baseline_slicer_runtime_quarantine: inherited reference test design that cannot patch immutable Slicer bindings or expects the headless model validator; retain assertions and exclude only from the Slicer gate.
